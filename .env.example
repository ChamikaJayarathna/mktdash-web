NEXT_PUBLIC_APP_URL=http://localhost:3000
PLAYWRIGHT_BASE_URL=http://localhost:3000

## ---------------------------------------------------------------------------
## Externally managed — do not set these in a local .env file
## ---------------------------------------------------------------------------

# CI: Automatically set by the CI provider (e.g. GitHub Actions).
# Accepted values: true | false.
CI=false

# NODE_ENV: Automatically set by Next.js/Vitest.
# Accepted values: development | test | production.
NODE_ENV=development
